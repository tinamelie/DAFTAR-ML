#!/usr/bin/env python
"""
This setup.py file is minimal and delegates to pyproject.toml.
It exists for backward compatibility with tools that don't fully
support pyproject.toml yet.
"""
from setuptools import setup

if __name__ == "__main__":
    setup()
